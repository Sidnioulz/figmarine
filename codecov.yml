component_management:
  default_rules:
    paths:
      - "apps/*"
      - "packages/*"

flags:
  pkg-cache:
    paths:
      - packages/cache/
  pkg-eslint-plugin-figma:
    paths:
      - packages/eslint-plugin-figma/
  pkg-logger:
    paths:
      - packages/logger/
  pkg-rest:
    paths:
      - packages/rest/